# Testing Strategy

- **Visual Verification:** Use the Antigravity Browser Preview or VS Code Markdown Preview.
- **Automation:** Trigger workflows manually to verify `assets/` updates before the cron runs.
- **Mobile Check:** Verify table widths (`width="50%"`) don't break on narrow screens.
